// La entidad Producto se crea solo para tener una idea de cómo será, pero como
// estamos trabajando con Prisma, no es necesario.

export class Product {
  public id: number; // 1, 2, 3, 4, 5, 6
  public name: string;
  public price: number;
}
